# Anthropic API Key (Required)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# xMCP Server URL (Optional - defaults to http://localhost:3001/mcp)
# Only set this if your xMCP server runs on a different URL
MCP_SERVER_URL=http://localhost:3001/mcp

# Clerk Authentication (Required)
# Get your keys from: https://dashboard.clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Clerk Webhook Secret (Optional - for syncing users)
CLERK_WEBHOOK_SECRET=whsec_...

# Database URL (Required for production)
# For Vercel deployment, use Vercel Postgres or external PostgreSQL
# Example: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@host:port/database

# Mux Video Platform (Required for video uploads)
# Get your credentials from: https://dashboard.mux.com/
MUX_TOKEN_ID=your-mux-token-id
MUX_TOKEN_SECRET=your-mux-token-secret
MUX_WEBHOOK_SIGNING_SECRET=your-webhook-signing-secret

# App URL (Required for Mux CORS and webhooks)
NEXT_PUBLIC_APP_URL=http://localhost:3000